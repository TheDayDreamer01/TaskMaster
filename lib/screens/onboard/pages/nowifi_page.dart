import "package:flutter/material.dart";


class NoWifiPage extends StatelessWidget {
  const NoWifiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body : Center(
        child : Text("No Wifi Page")
      )
    );
  }
}